# CSD Solo Miner — Python 3.9+ 依赖
# 只需运行: pip install -r requirements.txt

# Async HTTP 客户端（节点 RPC 调用）
aiohttp>=3.9.0

# YAML 配置文件解析
PyYAML>=6.0

# 数值计算（用于哈希评分）
numpy>=1.24.0

# 注意：本版本（v5.0.0）已移除 GPU 硬依赖
# 程序将使用 CPU 模式运行，无需安装 CUDA 或显卡驱动
# 若需 GPU 加速，可手动安装: pip install cupy-cuda12x
